package com.lagradost.cloudstream3.ui.download

/** Downloads are intentionally disabled in the Movies module for now. */
object DownloadButtonSetup {
    fun handleDownloadClick(click: DownloadClickEvent) = Unit
}
