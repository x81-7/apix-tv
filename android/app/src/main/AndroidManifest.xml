<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Permissions -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <!-- Required by AdMob 21+ for Android 13+ to read the advertising ID -->
    <uses-permission android:name="com.google.android.gms.permission.AD_ID" />

    <!-- TV Feature (not required) -->
    <uses-feature android:name="android.software.leanback" android:required="false" />
    <uses-feature android:name="android.hardware.touchscreen" android:required="false" />

    <application
        android:name=".ApixApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:banner="@drawable/tv_banner"
        android:supportsRtl="true"
        android:theme="@style/Theme.TVPlus"
        android:usesCleartextTraffic="true"
        tools:targetApi="31">

        <!-- AdMob App ID — value is rewritten in-place by the dashboard via update-android-config -->
        <meta-data
            android:name="com.google.android.gms.ads.APPLICATION_ID"
            android:value="ca-app-pub-4070921276882121~6667425901" />
        <!-- Splash Activity (Entry Point - Security Check) -->
        <activity
            android:name=".SplashActivity"
            android:exported="true"
            android:theme="@style/Theme.TVPlus.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
                <category android:name="android.intent.category.LEANBACK_LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Gate Activity (front entry screen, optional) -->
        <activity
            android:name=".GateActivity"
            android:exported="false"
            android:configChanges="orientation|screenSize|keyboardHidden|layoutDirection"
            android:windowSoftInputMode="adjustResize"
            android:theme="@style/Theme.TVPlus.NoActionBar" />

        <!-- About / Privacy screen -->
        <activity
            android:name=".AboutActivity"
            android:exported="false"
            android:configChanges="orientation|screenSize|keyboardHidden|layoutDirection"
            android:theme="@style/Theme.TVPlus.NoActionBar" />

        <!-- Compose Main Activity (NEW - Jetpack Compose UI) -->
        <activity
            android:name=".ComposeActivity"
            android:exported="true"
            android:configChanges="orientation|screenSize|keyboardHidden|layoutDirection"
            android:theme="@style/Theme.TVPlus.NoActionBar">
            <!-- Custom scheme for external encrypted payload (apix://<payload>) -->
            <intent-filter android:autoVerify="false">
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="apix" />
            </intent-filter>
            <!-- HTTPS deep link from the web launcher: https://apix-panal.vercel.app/watch.html?id=<payload>&app=com.apix.app -->
            <intent-filter android:autoVerify="false">
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="https"
                      android:host="apix-panal.vercel.app"
                      android:pathPrefix="/watch" />
            </intent-filter>
            <!-- Intent URI fallback (intent://...#Intent;scheme=apix;package=com.apix.app;end) -->
            <intent-filter>
                <action android:name="com.apix.app.OPEN_EXTERNAL" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>

        <!-- Legacy Home Activity (kept for backward compatibility) -->
        <activity
            android:name=".HomeActivity"
            android:exported="false"
            android:configChanges="orientation|screenSize|keyboardHidden"
            android:theme="@style/Theme.TVPlus.NoActionBar" />

        <!-- Sub Menu Activity (legacy) -->
        <activity
            android:name=".SubMenuActivity"
            android:exported="false"
            android:configChanges="orientation|screenSize|keyboardHidden"
            android:theme="@style/Theme.TVPlus.NoActionBar" />

        <!-- Player Activity (legacy ExoPlayer) -->
        <activity
            android:name=".PlayerActivity"
            android:exported="false"
            android:configChanges="orientation|screenSize|keyboardHidden"
            android:screenOrientation="sensorLandscape"
            android:theme="@style/Theme.TVPlus.Fullscreen" />

        <!-- WebView Activity -->
        <activity
            android:name=".WebViewActivity"
            android:exported="false"
            android:configChanges="orientation|screenSize|keyboardHidden"
            android:theme="@style/Theme.TVPlus.NoActionBar" />

        <!-- Old MainActivity -->
        <activity
            android:name=".MainActivity"
            android:exported="false"
            android:theme="@style/Theme.TVPlus.NoActionBar" />

        <!-- Kill Screen (TEMP_BAN / PERMA_BAN / TAMPERED_MOD) -->
        <activity
            android:name=".KillScreenActivity"
            android:exported="false"
            android:excludeFromRecents="true"
            android:noHistory="true"
            android:launchMode="singleTask"
            android:configChanges="orientation|screenSize|keyboardHidden|layoutDirection"
            android:theme="@style/Theme.TVPlus.NoActionBar" />

        <!-- FileProvider for internal APK install -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.updateprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_provider_paths" />
        </provider>

        <!-- (Removed) NotificationListenerService + BootReceiver — no more "جاهز لاستقبال" persistent notification.
             Realtime listener now runs in-process from ApixApplication.onCreate(). -->

        <activity
            android:name=".WebAdActivity"
            android:exported="false"
            android:configChanges="orientation|screenSize|keyboardHidden"
            android:theme="@style/Theme.AppCompat.NoActionBar" />
        <activity
            android:name=".ActivationActivity"
            android:exported="false"
            android:configChanges="orientation|screenSize|keyboardHidden"
            android:theme="@style/Theme.AppCompat.NoActionBar" />

    </application>

</manifest>
